<%- include("../partials/header") %>
	<div class="ui container segment">
		<div class="ui icon header">Enter a comment for <%= foundBlog.blogTitle %></div>
		<form class="ui form" action="/index/<%= foundBlog._id %>/comments/<%=comment._id %>?_method=PUT" method="POST">
			  <div class="field">
				<label>Title</label>
				<input type="text" name="comment[title]" placeholder="Title" value="<%= comment.title %>">
			  </div>
			<div>
			</div>
			  <button class="ui button" type="submit">Submit</button>
</form>
	</div>
<%- include("../partials/footer") %>
